import * as React from 'react';
import * as styles from './styles.css';

export default class Icon extends React.Component<any, any> {
  render() {
    return (
      <div className={styles.icon}>sup</div>
    );
  }
}
